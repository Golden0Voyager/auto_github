# 🌌 GitHub 开源趋势 & LLM 大厂动态周报

> **分析画像**: {{ persona.name }} (`{{ persona.description }}`)  
> **生成时间**: {{ timestamp }}  
> **精选指标**: 已从 {{ total_input }} 个热门候选项目中筛选出 {{ repos | length }} 个硬核更新。

---

## 📊 今日热门看板 (Dashboard)

> [!TIP]
> 点击下方各类别即可展开/收起项目列表看板。星标数与热度已实现标准化对齐。

{% macro repo_card(repo) %}
| {{ '**`'+repo.rating+'`**' if repo.rating else '' }} | [{{ repo.full_name }}]({{ repo.url }}) | `{{ repo.language }}` | ⭐️ {{ "{:,}".format(repo.stars) }} ({{ repo.period_stars }}) | {% for t in repo.tags %}`{{ t }}` {% endfor %} | {{ repo.description_short }} |
{% endmacro %}

{% set buckets = repos | groupby('_bucket') %}
{% for bucket_key in ['early_bird', 'high_star', 'deep_dive'] %}
{% set bucket_repos = repos | selectattr('_bucket', 'equalto', bucket_key) | list %}
{% if bucket_key == 'early_bird' %}
<details open>
  <summary><b>📅 每日热门趋势 (Daily Trending) [共计 {{ bucket_repos | length }} 个项目]</b></summary>
  <br>

| 评级 | 项目名称 | 主要语言 | 社区热度 | 核心技术标签 | 项目简介 |
| :---: | :--- | :---: | :---: | :--- | :--- |
{% for r in bucket_repos %}{{ repo_card(r) }}
{% endfor %}
  <br>
</details>
{% elif bucket_key == 'high_star' %}
<details>
  <summary><b>📅 每周热门趋势 (Weekly Trending) [共计 {{ bucket_repos | length }} 个项目]</b></summary>
  <br>

| 评级 | 项目名称 | 主要语言 | 社区热度 | 核心技术标签 | 项目简介 |
| :---: | :--- | :---: | :---: | :--- | :--- |
{% for r in bucket_repos %}{{ repo_card(r) }}
{% endfor %}
  <br>
</details>
{% elif bucket_key == 'deep_dive' %}
<details>
  <summary><b>📅 每月热门趋势 (Monthly Trending) [共计 {{ bucket_repos | length }} 个项目]</b></summary>
  <br>

| 评级 | 项目名称 | 主要语言 | 社区热度 | 核心技术标签 | 项目简介 |
| :---: | :--- | :---: | :---: | :--- | :--- |
{% for r in bucket_repos %}{{ repo_card(r) }}
{% endfor %}
  <br>
</details>
{% endif %}
{% endfor %}

---

## 🔍 深度项目解析

{% for r in repos %}
### {% if r.rating == 'S' %}👑 {% elif r.rating == 'A' %}🔥 {% else %}🔹 {% endif %}[{{ r.rating }}] {{ r.full_name }}
- **项目地址**: [{{ r.url }}]({{ r.url }})
- **基本数据**: `{{ r.language }}` | ⭐️ {{ "{:,}".format(r.stars) }} ({{ r.period_stars }}) | **来源**: `{{ r.source }}`
- **入选理由**: *{{ r.selection_reason }}*

{{ r.chinese_summary }}

---

{% endfor %}

> **Token Economics**: multi-model curation pipeline via OpenRouter.

*Designed with ❤️ by Antigravity Curation System.*

---

🤖 LLM 调用: {{ llm_stats.call_count }} 次成功 / {{ llm_stats.failed_count }} 重试失败 · {{ llm_stats.tokens }} tokens ({{ llm_stats.prompt_tokens }} in / {{ llm_stats.completion_tokens }} out)
